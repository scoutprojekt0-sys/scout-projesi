name: Health Monitor

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check-health:
    runs-on: ubuntu-latest
    steps:
      - name: Check staging health
        run: |
          if [ -z "${STAGING_HEALTHCHECK_URL}" ]; then
            echo "STAGING_HEALTHCHECK_URL is not configured, skipping."
            exit 0
          fi
          curl --retry 3 --retry-delay 5 --max-time 15 -fsS "${STAGING_HEALTHCHECK_URL}" > /dev/null
        env:
          STAGING_HEALTHCHECK_URL: ${{ secrets.STAGING_HEALTHCHECK_URL }}

      - name: Check production health
        run: |
          if [ -z "${PROD_HEALTHCHECK_URL}" ]; then
            echo "PROD_HEALTHCHECK_URL is not configured, skipping."
            exit 0
          fi
          curl --retry 3 --retry-delay 5 --max-time 15 -fsS "${PROD_HEALTHCHECK_URL}" > /dev/null
        env:
          PROD_HEALTHCHECK_URL: ${{ secrets.PROD_HEALTHCHECK_URL }}

      - name: Notify Slack on failure
        if: failure()
        run: |
          payload='{"text":"NextScout health monitor failed. Check Actions: '${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}'"}'
          curl -X POST -H "Content-type: application/json" --data "${payload}" "${ALERT_SLACK_WEBHOOK_URL}"
        env:
          ALERT_SLACK_WEBHOOK_URL: ${{ secrets.ALERT_SLACK_WEBHOOK_URL }}
